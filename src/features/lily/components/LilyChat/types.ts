// Re-export types from parent LilyChat component types
export * from '../types';

// Additional types for LilyChat folder structure
export { MIN_HISTORY_WIDTH, MAX_HISTORY_WIDTH, DEFAULT_HISTORY_WIDTH } from '../types';
