---
description: 새 기능 개발 시 스킬들을 단계별로 오케스트레이션
---

# 종합 개발 워크플로우

> [!CAUTION]
> **다중 요청 시 일괄 커밋 필수**
> 사용자가 여러 개를 요청하면 **모든 수정 완료 후** 한 번에 빌드/커밋/푸시

이 워크플로우는 모든 스킬을 단계별로 조합하여 새 기능 개발을 체계적으로 수행합니다.

## 🎯 워크플로우 흐름

```
[요구사항] → [분석] → [설계] → [구현] → [테스트] → [배포]
     ↓          ↓         ↓         ↓          ↓         ↓
 supabase   refactoring  db-migration  react   testing   deploy
             skill        skill      component  skill   workflow
```

---

## 1단계: 분석 및 설계

### 요구사항 분석
- 기능 범위 정의
- 영향받는 파일/모듈 식별
- 리팩토링 필요 여부 확인 (/refactoring 스킬 참조)

### DB 스키마 설계
- 새 테이블 필요 여부 확인
- FK 규칙 결정 (/db-migration 스킬 참조)
- RLS 정책 설계

---

## 2단계: DB 마이그레이션 (필요시)

// turbo
```bash
# 마이그레이션 생성
supabase migration new [migration_name]
```

**체크리스트** (/db-migration 스킬):
- [ ] FK에 ON DELETE CASCADE/SET NULL 명시
- [ ] RLS 활성화
- [ ] 인덱스 생성
- [ ] delete-users 업데이트 필요 여부

// turbo
```bash
# 로컬 적용 테스트
supabase db reset
```

---

## 3단계: 프론트엔드 구현

### 컴포넌트 개발 (/react-component 스킬)
- shadcn/ui 컴포넌트 사용
- Zustand 상태 관리
- i18n 번역 키 사용

### 리팩토링 (/refactoring 스킬)
- 대형 파일 분할 (>300줄)
- 폴더 구조화
- Re-export shim 생성

---

## 4단계: 테스트 (/testing 스킬)

### TypeScript 검증
// turbo
```bash
npx tsc --noEmit
```

### 빌드 테스트
// turbo
```bash
npm run build
```

### 단위 테스트 (선택)
// turbo
```bash
npx vitest run
```

---

## 5단계: 성능 확인 (/performance 스킬)

### 번들 크기 확인
// turbo
```bash
npm run build 2>&1 | tail -10
```

**경고 기준**:
- react-vendor: >70KB gzip
- 페이지 청크: >80KB gzip

---

## 6단계: 배포 (/deploy 워크플로우)

// turbo
```bash
git add -A && git commit -m "feat: [기능명]" && git push
```

### Edge Function (필요시)
// turbo
```bash
supabase functions deploy [function-name]
```

### DB 마이그레이션 (필요시)
// turbo
```bash
supabase db push
```

---

## ⚠️ 주의사항

| 상황 | 조치 |
|------|------|
| 대형 파일 분할 | 사전 백업, 단계적 진행 |
| DB 마이그레이션 | 프로덕션 적용 전 로컬 테스트 |
| 브라우저 테스트 필요 | **반드시 사용자 확인** |

---

## 빠른 참조

| 스킬 | 용도 |
|------|------|
| /db-migration | FK 규칙, RLS 패턴 |
| /react-component | 컴포넌트 개발 |
| /refactoring | 모듈화, 분할 |
| /testing | Vitest, 테스트 전략 |
| /performance | 번들 최적화 |
| /supabase | 스키마, Edge Function |
